#include <bits/stdc++.h>

using namespace std;

void print()
{
  cout << "raj";
}

int add(int a, int b)
{
  return (a + b);
}
int main()
{
  int a, b;
  cin >> a >> b;
  cout << add(a, b);
  return 0;
}